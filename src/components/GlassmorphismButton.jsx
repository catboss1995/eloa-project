import React from 'react'
import "../scss/styleGlassmorphismButton.scss"

const GlassmorphismButton = ({text}) => {
  return (
    <div className="glass-button">{text}</div>
  )
}

export default GlassmorphismButton